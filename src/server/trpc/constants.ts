/** Cookie set by the app shell when the user picks an active workspace. */
export const ACTIVE_ORGANIZATION_COOKIE = "active_organization_id";

/** Optional header override (e.g. mobile or non-browser clients). */
export const ACTIVE_ORGANIZATION_HEADER = "x-organization-id";
